apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: envoy-gateway
  namespace: flux-system
spec:
  interval: 1h
  chart:
    spec:
      chart: gateway-helm
      version: "1.8.3"
      sourceRef:
        kind: HelmRepository
        name: envoy-gateway
        namespace: flux-system
  targetNamespace: envoy-gateway-system
