apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: rampme-backend
  namespace: rampme
spec:
  imageRepositoryRef:
    name: rampme-backend
  filterTags:
    pattern: '^prod-(?P<ts>[0-9]+)$'
    extract: '$ts'
  policy:
    numerical:
      order: asc
