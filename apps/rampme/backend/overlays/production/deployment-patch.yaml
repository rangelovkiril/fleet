apiVersion: apps/v1
kind: Deployment
metadata:
  name: rampme-backend
spec:
  template:
    spec:
      containers:
        - name: backend
          image: ghcr.io/rangelovkiril/hacktues12-backend:prod-20260903110042 # {"$imagepolicy": "rampme:rampme-backend"}
          envFrom:
            - secretRef:
                name: rampme-backend-secret
