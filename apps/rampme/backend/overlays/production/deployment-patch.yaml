apiVersion: apps/v1
kind: Deployment
metadata:
  name: rampme-backend
spec:
  template:
    spec:
      containers:
        - name: backend
          image: ghcr.io/rangelovkiril/rampme-backend:prod-20260905090107 # {"$imagepolicy": "rampme:rampme-backend"}
          envFrom:
            - secretRef:
                name: rampme-backend-secret
