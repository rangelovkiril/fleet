apiVersion: apps/v1
kind: Deployment
metadata:
  name: rampme-backend
spec:
  template:
    spec:
      containers:
        - name: backend
          image: ghcr.io/rangelovkiril/rampme-backend:prod-20260908170020 # {"$imagepolicy": "rampme:rampme-backend"}
          envFrom:
            - secretRef:
                name: mqtt-broker-credentials
