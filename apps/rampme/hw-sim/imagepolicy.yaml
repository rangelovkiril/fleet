apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: hw-sim
  namespace: rampme
spec:
  imageRepositoryRef:
    name: hw-sim
  filterTags:
    pattern: '^stage-(?P<ts>[0-9]+)$'
    extract: '$ts'
  policy:
    numerical:
      order: asc
