apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: rampme-hw-sim
  namespace: rampme
spec:
  imageRepositoryRef:
    name: rampme-hw-sim
  filterTags:
    pattern: '^stage-(?P<ts>[0-9]+)$'
    extract: '$ts'
  policy:
    numerical:
      order: asc
